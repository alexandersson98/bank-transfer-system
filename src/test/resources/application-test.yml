spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_db_test
    username: postgres
    password: chasacademy
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true